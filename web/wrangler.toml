# wrangler.toml — Cloudflare Pages configuration
# Deploy with: npx wrangler pages deploy --project-name=arkanoid

name = "arkanoid"
pages_build_output_dir = "./dist"
compatibility_date = "2024-01-01"
